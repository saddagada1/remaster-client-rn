mutation ChangeUsername($username: String!) {
  changeUsername(username: $username) {
    errors {
      field
      message
    }
    user {
      id
      verified
      email
      username
      created_at
      updated_at
    }
  }
}

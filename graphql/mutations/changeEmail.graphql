mutation ChangeEmail($email: String!) {
  changeEmail(email: $email) {
    errors {
      field
      message
    }
    user {
      id
      verified
      email
      username
      created_at
      updated_at
    }
  }
}
